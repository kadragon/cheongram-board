# Current Active Tasks
# Only ONE task should be in_progress at any time

meta:
  last_updated: 2025-11-06
  format_version: 1.0
  status: active

# Active Task
current_task:
  id: TASK-current-001
  title: Refactor all MD files to SDD × TDD structure
  trace: SPEC-governance-refactor-1
  description: |
    Reorganize all markdown documentation files into proper .spec/, .tasks/,
    and .governance/ structure following SDD × TDD principles.
  acceptance_criteria:
    - All .governance/ files created (memory.md, patterns.md, coding-style.md, env.yaml)
    - All .tasks/ files converted to YAML (backlog.yaml, current.yaml, done.yaml)
    - Log files moved to .tasks/logs/
    - .spec/ files organized with SUMMARY.md
    - Redundant files archived or removed
  priority: high
  estimated_effort: 4 hours
  actual_effort: 2.5 hours (so far)
  status: in_progress
  started_at: 2025-11-06T12:00:00Z
  progress:
    completed:
      - Find all .md files in project
      - Analyze and classify each file
      - Create .governance/memory.md
      - Create .governance/patterns.md
      - Create .governance/coding-style.md
      - Create .governance/env.yaml
      - Create .tasks/backlog.yaml
      - Create .tasks/current.yaml
    in_progress:
      - Move log files to .tasks/logs/
    pending:
      - Create .tasks/done.yaml
      - Create .spec/ SUMMARY files
      - Archive redundant files
      - Update README.md references
  dependencies: []
  blocked_by: []
  owner: claude-code-agent

# Recently Completed (moved from current)
recently_completed: []

# Upcoming (next in queue from backlog)
upcoming:
  - id: TASK-backlog-001
    title: Resolve .dev.vars environment variable configuration
    priority: critical
    estimated_effort: 0.5 hours

# Notes
notes:
  - Only one task should be in_progress at a time
  - Move completed tasks to done.yaml immediately
  - Update progress regularly (at least daily during active work)
  - If blocked, create blocker entry and update status
